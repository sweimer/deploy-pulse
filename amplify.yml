version: 0.2
frontend:
  phases:
    preBuild:
      runtime-versions:
        nodejs: 20
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
